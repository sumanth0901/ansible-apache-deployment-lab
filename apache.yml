---
- name: Apache Deployment
  hosts: webservers
  become: true

  tasks:
    - name: Install Apache
      dnf:
        name: httpd
        state: present

    - name: Start and Enable Apache
      service:
        name: httpd
        state: started
        enabled: yes
